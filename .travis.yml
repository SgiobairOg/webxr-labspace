language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
script:
  - npm run build
deploy:
  os: linux
  provider: pages
  cleanup: true
  token: $token
  local_dir: dist
  on:
    branch: master